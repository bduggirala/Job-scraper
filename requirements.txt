# Runtime dependencies. Upper bounds are deliberate: this pipeline parses
# third-party HTML and JSON, and a surprise major release of pandas, lxml or
# BeautifulSoup changes parsing behaviour in ways that show up as silently
# wrong job data rather than as an import error.
pandas>=2.0,<3.0
openpyxl>=3.1,<4.0
requests>=2.31,<3.0
playwright==1.62.0
pyyaml>=6.0,<7.0
tenacity>=8.2,<10.0
beautifulsoup4>=4.12,<5.0
lxml>=5.0,<7.0
python-dateutil>=2.8,<3.0
playwright-stealth==2.0.3
